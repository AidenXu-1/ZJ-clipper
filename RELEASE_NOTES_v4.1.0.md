# 兆基clipper v4.1.0

这一版新增知乎精准抓取，并修复部分网页上扩展弹窗偏宽、右侧留白的问题。

## 普通用户下载

- **Chrome**：下载 `zhaoji-clipper-chrome.zip`
- **Edge**：下载 `zhaoji-clipper-edge.zip`

带 `4.1.0` 版本号的附件内容相同，用于历史归档和复现。

## 新增知乎抓取

- 支持知乎专栏文章、单条回答、问题说明和单条想法。
- 专栏会保留开头封面、可见标题与正文图片；兼容新版知乎把封面放在文章主体之外的页面结构。
- 回答按当前 URL 中的回答 ID 精确锁定，不会误抓同页其它回答。
- 排除评论区、推荐内容和作者头像，保留懒加载图片真实地址、作者、日期、话题标签与可见互动数据。
- 想法中的外置图集也会随正文一起保存。

## 弹窗显示修复

- 弹窗宽度固定为 420px，纵向滚动收进面板内部。
- 长 URL 不再把弹窗撑到浏览器宽度上限。
- 修复浏览器在弹窗右侧额外增加滚动条空隙造成的视觉偏移。

## 安装或升级

1. 下载对应浏览器的 ZIP 并解压。
2. Chrome 打开 `chrome://extensions`，Edge 打开 `edge://extensions`。
3. 新安装：开启开发者模式，选择“加载已解压的扩展程序”。
4. 已有用户：为保留原有本地设置，请用新版文件覆盖原先加载的扩展目录，再点击“重新加载”并刷新网页；不要先移除旧扩展。

## 发布验证

- TypeScript 类型检查通过。
- Chrome 与 Edge 正式构建、ZIP 归档通过。
- 内容脚本纯 ASCII 检查通过。
- 知乎离线回归覆盖专栏新旧封面结构、回答 ID 锁定、问题说明、想法外置图集及头像/推荐内容排除。
- 真实知乎页面已确认新版封面结构与文章级封面元数据一致。

真实浏览器中的最终保存结果仍受知乎页面更新、登录状态、Obsidian 与本地图片下载配置影响。
