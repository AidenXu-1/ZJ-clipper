# Changelog

## v4.1.0 - 2026-08-12

新增知乎精准抓取，并修复扩展弹窗在部分页面出现偏宽、右侧留白的问题。

### 弹窗显示

- 固定扩展弹窗外层宽度为 420px，并把纵向滚动收进面板内部，避免长 URL 撑宽弹窗或浏览器在右侧额外增加滚动条空隙。

### 知乎抓取

- 新增知乎专用适配器，支持专栏文章、单条回答、问题说明与单条想法。
- 按 URL 锁定目标回答，只读已渲染正文，排除同页其它回答、评论区和推荐内容。
- 支持知乎懒加载图片真实地址、作者、日期、话题标签及可见互动数据。
- 修复目标回答未渲染时误抓其它回答、想法外置图集遗漏，以及问题日志等子页面被误判为问题正文。
- 专栏会保留页面开头的封面和可见标题；兼容新版封面位于文章主体之外、只通过文章元数据声明的结构。

## v4.0.2 - 2026-07-15

面向公开体验用户的纯净发布与仓库整理版本。

### 纯净体验包

- 新安装不再预设 `woshipm.com` → `PM` 网站标签规则，仓库、标签规则和各类凭证均由用户自行配置。
- Chrome / Edge Release 附件继续只包含浏览器扩展运行文件，不包含源码、开发文档、缓存或本机配置。
- 固定最新版文件名 `zhaoji-clipper-chrome.zip`、`zhaoji-clipper-edge.zip` 与带版本号归档包同时发布。

### 仓库与接力开发

- 许可证明确允许个人、非商业安装与使用官方编译版，并保留源码修改、再分发和商业使用限制。
- 补齐并校正 v1.0.0、v2.0.1、v3.0.0、v4.0.0 的历史说明。
- 功能自查清单移动到 `docs/TESTING.md`，统一作为人工回归入口。
- README 增加版本、CI、许可状态与纯净体验包说明。
- Dependabot 跳过已经确认不兼容的工具链大版本，避免重复产生无法通过构建的升级 PR。

## v4.0.1 - 2026-07-09

飞书抓取重复问题修复版本。

### 飞书抓取修复

- **Grid 图片去重**：修复 Grid 布局下同一图片在 Markdown 多个位置重复出现的问题，新增 `dedupeImagesByGlobalUrl` 基于 URL 全局去重。
- **滚动容器修复**：修复 `findFeishuDocumentScroller` 误判侧边栏滚动容器导致尾部内容遗漏（如第4个 callout）的问题，优先选择正文编辑器容器。
- **非连续重复块去重**：`dedupeFeishuMarkdownRepeats` 新增单块签名去重，删除非连续位置的重复块。
- **列表子树去重**：`normalizeMarkdownListSubtree` 移除图片和双链差异，正确识别内容相同的重复列表子树。

## v4.0.0 - 2026-06-30

相较于 v3.0.0，本版本新增「一键同步飞书 / 存入飞书知识库」保存方式。

历史说明：v4.0.0 发布期间曾在同一版本号内补充飞书抓取修复。自 v4.0.1 起执行不可变发布，每次安装包内容变化都使用新的版本号，不再覆盖或移动已经发布的 Tag。

### 飞书知识库

- 新增保存方式 `飞书知识库`：用「自建应用」+ OAuth 用户授权，把剪藏内容一键同步为飞书文档，并移入指定知识库（wiki space）的指定节点位置。
- 设置页飞书卡片：填应用凭证、登录授权、加载知识库下拉、节点级联选择器（面包屑下钻 + 保存到当前层级/页面下）。
- 弹窗顶部「保存到」可在 Obsidian 仓库与飞书知识库之间一键切换；保存后可「在飞书打开」。
- 图片：公网图片通常由飞书导入器解析；飞书等需登录的鉴权图、blob 图暂不承诺随文上传，可能受飞书导入能力限制。
- 凭证（App Secret / OAuth token）与仓库配置档一致，仅存 `chrome.storage.local`，不进同步。
- 说明：飞书写入是用户显式开启的上传通道，是对 capture-only 边界的有意例外。

### 飞书抓取修复

- 修复飞书文档 bullet/list 在虚拟滚动场景下被重复抓取的问题，尤其是嵌套小节被浏览器临时提升为顶层节点时的重复输出。
- 修复长飞书画板截图混入正文、标题浮层或底部白条的问题，多段截图会隐藏干扰元素并跳过重叠区域。
- 增强诊断信息，便于定位复杂飞书文档中重复片段、图片布局、画板截图和 Defuddle 回退差异。

## v3.0.0 - 2026-06-21

多仓库切换、图片双模式与飞书图文排版版本。

### 多仓库与图片

- 新增仓库配置档，可在弹窗中切换多套 Obsidian 保存目标。
- 每个仓库独立保存仓库名、保存方式、REST 地址、API Key 和默认文件夹。
- 新增「下载到本地 / 引用原链接」图片双模式；鉴权图片仍会按需下载。

### 飞书与属性

- 保留飞书并排图片及其图注排版，修复标题与折叠内容提取问题。
- 「学习状态」改为独立 frontmatter 字段，不再混入 tags。
- 移除插件自动生成关键词的行为，语义整理交给下游工作流。

## v2.0.1 - 2026-06-15

安全与发布流程加固版本。

> 历史说明：该版本号曾进入 `main`，但没有创建对应 Tag 和 GitHub Release。相关修复随后包含在 v3.0.0 及更高版本中。

### Security

- Local REST API Key 从 `chrome.storage.sync` 迁移到当前设备的 `chrome.storage.local`，并自动清除旧同步副本。
- 增加常见密钥与本机配置文件的 Git 忽略规则。
- GitHub Actions 固定到官方发布提交 SHA，降低工作流供应链风险。
- CI 增加生产依赖安全审计；当前生产依赖漏洞为 0。
- 覆盖修复 `tar`、`shell-quote`、`tmp`、`uuid` 间接开发依赖，开发依赖 critical 漏洞降为 0。
- 增加 Dependabot 配置和私密漏洞报告说明。

## v2.0.0 - 2026-06-15

兆基clipper 2.0 重点优化飞书文档与 X / Twitter 长文章的剪存质量。

### 飞书文档

- 飞书画板无法直接转换为 Obsidian Markdown 时，自动截取为 PNG 图片，并插入原文对应位置。
- 修复飞书高亮块、标题、引用和 Callout 转换异常及多余空行问题。
- 支持提取可见的文档作者与最后修改日期，并写入笔记属性。

### X / Twitter

- 新增长文章 Article 正文提取，不再只保存图片而缺失中文文字。
- 「完整抓取全文」仅滚动当前文章区域，到正文最后一行即停止，避免误抓评论与其他推文。
- 保留普通推文和 thread 的原有剪存能力。

### 自动标签

- 每篇剪存默认添加 `unread` 标签，可在保存前勾选「本篇已学习」切换为 `已学习`。
- 支持按网站域名自动添加标签，默认内置 `woshipm.com` → `PM`，并可在设置页继续扩展。

## v1.0.0 - 2026-06-07

兆基clipper 第一个成品版本。

> 历史说明：GitHub Release 与 Tag 使用 `v1.0.0`，当时安装包内部版本号为 `0.1.0`。

### Added

- 支持 Chrome / Edge MV3 扩展形态。
- 支持 Obsidian `obsidian://` URI 保存。
- 支持 Obsidian Local REST API 保存超长内容与本地图片。
- 支持飞书文档、B站、YouTube、小红书、X / Twitter、微信公众号、Reddit 与通用网页剪藏。
- 支持选区剪藏、完整滚动抓取、X thread 抓取和网页划词高亮。
- 支持 frontmatter、标签、作者、发布时间、来源 URL、描述等属性编辑。
- 支持每篇独立文件夹、图片随文归档、文件名模板、标签黑名单、常用标签复用。
- 支持暗色主题和设置页。
- 支持诊断信息复制，用于按真实页面结构修复站点适配器。

### Notes

- 产品边界为 capture-only，不做 AI 摘要、总结、改写或分类。
- 知乎、微博、Medium、Daily Note 追加等能力仍在后续规划中。
