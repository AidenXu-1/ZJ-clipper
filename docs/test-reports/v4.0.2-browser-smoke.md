# v4.0.2 浏览器加载冒烟测试记录

## 结论

Chrome 和 Edge 均能加载 v4.0.2 生产构建，扩展处于启用状态，弹窗能够正常渲染。本记录只覆盖浏览器加载链路，不替代完整功能回归。

## 测试基线

- 日期：2026-07-15
- 扩展源码基线：`634701756315f758d4f68d0ee0e3df682e7b008c`
- Manifest 版本：`4.0.2`
- Chrome / Edge Manifest SHA-256：`557ba7ecfe8b7911914f8440fa257246656e553bea56d12f4f86e056d6fe3470`
- 系统：macOS 26.4.1（arm64）
- Chrome：150.0.7871.115
- Edge：150.0.4078.65

测试前已执行：

```bash
npm run compile
npm run build
npm run build:edge
```

三项命令均成功。

## Chrome

测试对象：`.output/chrome-mv3`

1. 在 `chrome://extensions` 中重新加载当前生产构建。
2. 扩展详情显示名称“Nomo Clipper”、版本 `4.0.2`、状态“已启用”。
3. 扩展详情中的 Service Worker 正常注册并可用。
4. 点击工具栏扩展图标，`popup.html` 成功打开。
5. 在浏览器内置页上，弹窗正确显示“当前页面无法剪藏”提示和“设置”按钮。

结果：通过。

## Edge

测试对象：`.output/edge-mv3`

1. 使用隔离的临时 Edge 配置启用开发人员模式并加载生产构建。
2. 扩展卡片显示名称“Nomo Clipper”、版本 `4.0.2`、状态开启。
3. MV3 Service Worker 已注册；空闲时显示“不活动”，没有显示加载错误。
4. `popup.html` 加载完成，页面标题为“Nomo Clipper”。
5. 在浏览器内置页上，弹窗正确显示“当前页面无法剪藏”提示和“设置”按钮。
6. 测试结束后关闭浏览器，并删除隔离配置及其临时数据。

结果：通过。

## 未覆盖项

本次没有验证：

- 飞书、B站、YouTube、小红书、X、公众号、Reddit 等真实页面抓取。
- Obsidian URI 与 Local REST API 保存。
- 飞书 OAuth、知识库选择、Token 刷新与文档导入。
- 图片下载、完整滚动抓取、划词高亮和旧版本设置迁移。
- Windows、Linux 以及浏览器商店安装。

以上项目仍以 [`docs/TESTING.md`](../TESTING.md) 中的未勾选状态为准。
