# 兆基clipper v3.0.0

本版本带来三大功能，并修复多个飞书剪藏的细节问题。

## 多仓库切换

- 新增「仓库配置档」：可保存多套保存目标，在剪藏弹窗顶部「保存到」下拉里一键切换、立即生效，不用再进设置页改、不用刷新。
- 每个仓库各自独立：仓库名、保存方式、Local REST API 地址/Key、默认保存文件夹互不影响。
- 修复 REST 方式保存后「在 Obsidian 打开」显示「未找到」的问题（每个仓库带仓库名用于打开跳转）。
- API Key 与仓库配置只保存在本机，不随浏览器账号同步。

## 图片处理双模式

- 「下载到本地」：图片下载进仓库，离线可看、自包含。
- 「引用链接」：公开图片只引用原网页链接、不占空间；飞书等需登录的图片会自动下载以保证显示。
- 后台下载按需携带 Cookie（仅飞书/Lark 等需登录站点），最小化暴露面。

## 飞书剪藏增强

- 并排图片：飞书里并排的图，在 Obsidian 阅读视图里也并排显示，图注居中显示在每张图正下方。
- 修复折叠在标题下的居中副标题被漏抓的问题。
- 修复文件命名偶尔取到「Docs」占位、而非真实文档标题的问题。

## 其它

- 「学习状态」改为独立的 frontmatter 字段（已学习 / 未学习），不再混进 tags，避免被下游工具重写关键词时冲掉。
- 关键词不再由插件自动生成，交给下游流程处理。

第一版 `v1.0.0`、第二版 `v2.0.0` 继续保留在 Releases 页面。
