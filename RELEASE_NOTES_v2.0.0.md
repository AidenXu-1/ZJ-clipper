# 兆基clipper 2.0

本次版本重点优化飞书文档与 X / Twitter 长文章的剪存质量。

## 1. 优化飞书存储格式

- 飞书画板此前因不兼容 Obsidian Markdown 无法抓取；现在会自动转换为 PNG 图片，并放置在原文对应位置。
- 修复飞书高亮块抓取后格式显示异常的问题。
- 同步改善飞书标题、引用、Callout、多余空行、作者和修改日期的处理。

## 2. 优化 X / Twitter 长文章抓取逻辑

- 此前普通推文可以抓取，但长文章通常只能保存图片、无法取得中文正文；现在可以完整抓取长文章文字与图片。
- 点击「完整抓取全文」后，只滚动当前文章正文，到正文最后一行即停止，不再误抓下方其他推文和评论。

## 3. 自动标签

- 剪存文章时默认添加 `unread` 标签。
- 保存前勾选「本篇已学习」，标签会切换为 `已学习`。
- 支持按网站域名自动添加标签，默认内置 `woshipm.com` → `PM`，也可在设置页自行扩展。

## 安装

下载本 Release 中的 Chrome / Edge 安装包并解压，在浏览器扩展管理页开启开发者模式，然后选择「加载已解压的扩展程序」。

第一版 `v0.1.0` 继续保留在 Releases 页面，可随时下载或查看源码。
