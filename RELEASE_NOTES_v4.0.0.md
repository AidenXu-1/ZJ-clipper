# 兆基clipper v4.0.0

相较于 v3.0.0，V4 的核心更新是把剪藏结果从 Obsidian 本地保存扩展到可选的飞书知识库保存，同时继续保持默认本地、显式授权、凭证本机保存的边界。

本次 GitHub V4 包已直接替换为当前稳定版：保留原 `v4.0.0` 版本号，不新增版本号，修正此前 V4 在飞书复杂文档抓取中的重复列表和长画板截图异常。

## 主要更新

- 一键同步飞书知识库：新增 `飞书知识库` 保存方式，用户配置自建应用并完成 OAuth 授权后，可把当前剪藏一键导入为飞书文档，并移动到指定知识库节点。
- 设置页飞书配置：支持 App ID / App Secret、国内版飞书与国际版 Lark 数据中心、飞书登录授权、知识库列表、节点级联选择，以及粘贴知识库/页面链接定位。
- 弹窗保存目标升级：Obsidian 保存目标与飞书知识库目标分开展示，可在弹窗中保存到 Obsidian 或直接保存到飞书，保存后可一键打开对应文档。
- 飞书文档抓取增强：改进飞书代码块、画板、图片、表格、高亮、引用、Callout 等内容的提取与 Markdown 转换，提升复杂飞书资料剪藏完整度。
- 飞书 bullet 重复修复：针对飞书虚拟滚动会把嵌套列表临时提升为顶层节点的问题，新增列表子树去重与 Markdown 窗口诊断，避免同一小节在 Obsidian 中重复出现。
- 飞书画板截图修复：导出超长画板时隐藏页面浮层、标题栏和水印干扰，并在多段截图拼接时跳过重叠区域，避免截图中混入正文或出现白色横条。
- 高亮工具增强：新增选中文字后的浮动高亮按钮开关，并把高亮入口整理到弹窗工具区。
- 安全边界更新：飞书 App Secret、OAuth access token / refresh token、Local REST API Key 与仓库配置档均只保存在 `chrome.storage.local`，不进入浏览器同步存储。
- 开发依赖加固：升级可安全 override 的 `tar`、`tmp` 等间接开发依赖，生产依赖审计为 0 漏洞。

## 隐私说明

默认保存方式仍是本地 Obsidian。只有用户主动选择「飞书知识库」、填写自建应用凭证、完成 OAuth 授权并点击保存时，剪藏内容才会直连上传到用户自己的飞书知识库；不经过本插件开发者服务器。
